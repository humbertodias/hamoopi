name: CD

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            platform_name: linux
          - os: macos-latest
            platform_name: macos
          - os: windows-latest
            platform_name: windows

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # --------------------
      # Configure CMake
      # --------------------
      - name: Configure
        shell: bash
        run: |
          EXTRA=""
          GENERATOR=""
      
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            EXTRA="-DCMAKE_POLICY_VERSION_MINIMUM=3.5"
          fi
      
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            GENERATOR="-G 'MinGW Makefiles'"
          fi
      
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release $EXTRA $GENERATOR

      # --------------------
      # Build
      # --------------------
      - name: Build
        run: |
          cmake --build build --config Release --parallel
        shell: bash

      # --------------------
      # Package
      # --------------------
      - name: Package
        run: |
          mkdir -p dist

          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            cp build/Release/HAMOOPI.exe dist/
          else
            cp build/HAMOOPI dist/
          fi

          cp -r data dist/
          cp SETUP.ini dist/
          cp README.md dist/
          cp LICENSE dist/

          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            cd dist
            7z a ../HAMOOPI-${{ matrix.platform_name }}.zip *
          else
            cd dist
            tar czf ../HAMOOPI-${{ matrix.platform_name }}.tar.gz *
          fi
        shell: bash

      # --------------------
      # Upload artifacts
      # --------------------
      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: HAMOOPI-${{ matrix.platform_name }}
          path: |
            HAMOOPI-${{ matrix.platform_name }}.tar.gz
            HAMOOPI-${{ matrix.platform_name }}.zip
          retention-days: 30

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v6
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
